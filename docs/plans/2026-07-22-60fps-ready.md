# BUNDLE 60fps-ready — motor fps-independente + blink gate com histerese

**Data:** 2026-07-22
**Contexto:** a câmera passou a entregar 60fps (constraint `ideal: 60` de `9fda6d0` finalmente honrada no desktop). Isso expôs: (a) EMA do trilho âmbar ingerindo frames de piscada (`if (gaze)` sem `!blinking` — bug real), (b) EMAs com alpha por-frame ficando 2× mais responsivos a 60fps, (c) gate de piscada binário (threshold 0.5) deixando passar 2× mais frames de borda por segundo.
**Fora de escopo:** calibração física px/cm do monitor (premissa 96dpi) — decidir depois como PACK próprio.
**Adendo de Anders (22/07):** o texto redimensionando ao vivo conforme a detecção é ruim de usar → Task 8: a fonte congela após a distância estabilizar (mesma política do player de exercícios); a detecção ao vivo passa a alimentar só o aviso de drift, nunca o tamanho.

## Checagem de escopo

Um subsistema só (motor de sinal ocular / blink gating). Plano único.

## Mapa de arquivos

| Arquivo | Ação | Responsabilidade |
|---|---|---|
| `src/services/emaTiming.ts` | Criar | Alpha de EMA compensado por dt (`1 − exp(−dt/τ)`) + constantes de τ |
| `src/services/emaTiming.test.ts` | Criar | Testes do helper |
| `src/services/blinkGate.ts` | Criar | Tracker stateful de piscada: histerese (entra 0.5, sai 0.25) + hold temporal + purge retroativo de amostras da borda de subida |
| `src/services/blinkGate.test.ts` | Criar | Testes do tracker e do purge |
| `src/hooks/useCameraPipeline.ts` | Modificar | Trocar `shouldDropGazeForBlink` pontual pelo tracker (estado por sessão de loop) |
| `src/screens/EyeTrackingTestScreen.tsx` | Modificar | Gatear EMA do trilho com `!blinking`; dt-compensar EMAs (trilho + distância); purge retroativo do traçado no onset da piscada; fonte congelada via `stimulusDistance` (Task 8) |
| `src/components/ExerciseCanvas.tsx` | Modificar | Tracker próprio no loop do exercício |
| `src/components/CalibrationOverlay.tsx` | Modificar | Tracker próprio no loop de calibração/validação |
| `src/services/stimulusDistance.ts` | Modificar | Opção `emaTauMs` (dt-compensado); default preserva comportamento legado |
| `src/services/stimulusDistance.test.ts` | Modificar | Teste de equivalência 30fps × 60fps |

Os helpers puros existentes (`isBlinking`, `shouldDropGazeForBlink`, `BLINK_REJECT_THRESHOLD`, `BLINK_REJECT_GATE_ENABLED` em `faceTracking.ts`) **permanecem exportados e intocados** — testes atuais continuam valendo; o tracker novo compõe sobre eles.

Nota honesta de validação: `EyeTrackingTestScreen.tsx` não tem teste unitário (screens não são testadas neste repo). A lógica nova entra como helpers puros testados (`emaTiming`, `blinkGate`); a fiação no screen é validada pelo gate final (build + smokes existentes).

---

### Task 1: Helper de EMA compensado por dt

**Arquivos:**
- Criar: `src/services/emaTiming.ts`
- Teste: `src/services/emaTiming.test.ts`

- [ ] **1. Escrever teste que falha**

```ts
// src/services/emaTiming.test.ts
import assert from 'node:assert/strict';
import { test } from 'node:test';
import { emaAlpha, RAW_V_EMA_TAU_MS, DISTANCE_EMA_TAU_MS } from './emaTiming';

test('emaAlpha reproduces the legacy per-frame alphas at 30fps', () => {
  // 0.98/0.02 do trilho âmbar a 33.3ms ⇒ τ ≈ 1650ms
  assert.ok(Math.abs(emaAlpha(100 / 3, RAW_V_EMA_TAU_MS) - 0.02) < 0.002);
  // 0.85/0.15 da distância a 33.3ms ⇒ τ ≈ 200ms
  assert.ok(Math.abs(emaAlpha(100 / 3, DISTANCE_EMA_TAU_MS) - 0.15) < 0.02);
});

test('emaAlpha halves responsiveness per step at 60fps vs 30fps', () => {
  const a60 = emaAlpha(100 / 6, RAW_V_EMA_TAU_MS);
  const a30 = emaAlpha(100 / 3, RAW_V_EMA_TAU_MS);
  assert.ok(a60 < a30 && a60 > a30 * 0.45 && a60 < a30 * 0.55);
});

test('same elapsed time converges the same regardless of frame rate', () => {
  // 1s de amostras constantes v=1 partindo de 0: 30 passos de 33.3ms × 60 de 16.7ms
  let ema30 = 0, ema60 = 0;
  for (let i = 0; i < 30; i++) ema30 += (1 - ema30) * emaAlpha(100 / 3, RAW_V_EMA_TAU_MS);
  for (let i = 0; i < 60; i++) ema60 += (1 - ema60) * emaAlpha(100 / 6, RAW_V_EMA_TAU_MS);
  assert.ok(Math.abs(ema30 - ema60) < 0.005);
});

test('emaAlpha guards non-positive dt', () => {
  assert.equal(emaAlpha(0, 1000), 0);
  assert.equal(emaAlpha(-16, 1000), 0);
});
```

- [ ] **2. Rodar e ver falhar** — `node --import tsx --test src/services/emaTiming.test.ts` → `Cannot find module './emaTiming'`
- [ ] **3. Implementação mínima**

```ts
// src/services/emaTiming.ts
// Frame-rate-independent EMA smoothing. The legacy per-frame alphas (0.02 and 0.15)
// were tuned at a 30fps camera; at 60fps they double the effective responsiveness.
// alpha = 1 − exp(−dt/τ) keeps the time constant τ true at any frame rate.

// τ that reproduces the legacy amber-rail alpha (0.02 at 33.3ms frames): ~1.5s rail.
export const RAW_V_EMA_TAU_MS = 1650;
// τ that reproduces the legacy distance alpha (0.15 at 33.3ms frames): ~0.2s.
export const DISTANCE_EMA_TAU_MS = 200;

export function emaAlpha(dtMs: number, tauMs: number): number {
  if (!(dtMs > 0) || !(tauMs > 0)) return 0;
  return 1 - Math.exp(-dtMs / tauMs);
}
```

- [ ] **4. Rodar e ver passar** — `node --import tsx --test src/services/emaTiming.test.ts`
- [ ] **5. Commit** (se autorizado): `feat: dt-compensated EMA helper for fps-independent smoothing`

---

### Task 2: Blink gate tracker — histerese + hold + purge retroativo

**Arquivos:**
- Criar: `src/services/blinkGate.ts`
- Teste: `src/services/blinkGate.test.ts`

Semântica (contrato):
- **Entra** em rejeição quando `score > enterThreshold` (0.5 — igual ao gate atual, preserva a defesa contra baseline alto do iPhone).
- **Permanece** rejeitando enquanto `score > exitThreshold` (0.25) — histerese pega a descida gradual da pálpebra.
- Ao sair da histerese, **hold temporal** (`holdMs` = 100ms) segue rejeitando — mata a borda de descida em qualquer fps (tempo, não frames).
- `score == null` continua **fail-open** para *entrar* (nunca inicia rejeição sem evidência), mas um null no meio de uma piscada transiciona para o hold (não reabre instantaneamente).
- `enabled: false` → nunca rejeita (mesma chave `BLINK_REJECT_GATE_ENABLED`).
- `purgeLeadingBlinkSamples` remove de um buffer `{t}` as amostras dos últimos `windowMs` (80ms) — a borda de *subida* que passou pelo gate antes do score cruzar 0.5.

- [ ] **1. Escrever teste que falha**

```ts
// src/services/blinkGate.test.ts
import assert from 'node:assert/strict';
import { test } from 'node:test';
import {
  createBlinkGateTracker,
  purgeLeadingBlinkSamples,
  BLINK_EXIT_THRESHOLD,
  BLINK_HOLD_MS,
  BLINK_LEADING_PURGE_MS,
} from './blinkGate';

test('tracker enters rejection only above the strict enter threshold', () => {
  const gate = createBlinkGateTracker();
  assert.equal(gate.update(0.3, 0), false);   // abaixo de 0.5: aberto
  assert.equal(gate.update(0.5, 16), false);  // limite não é piscada (compat gate atual)
  assert.equal(gate.update(0.6, 33), true);   // entrou
});

test('hysteresis keeps rejecting between exit and enter thresholds', () => {
  const gate = createBlinkGateTracker();
  gate.update(0.9, 0);
  assert.equal(gate.update(0.4, 16), true);   // 0.25 < 0.4 < 0.5: segue rejeitando
  assert.equal(gate.update(0.3, 33), true);
});

test('temporal hold keeps rejecting for holdMs after the score drops', () => {
  const gate = createBlinkGateTracker();
  gate.update(0.9, 0);
  assert.equal(gate.update(0.1, 20), true);                       // caiu → hold armado
  assert.equal(gate.update(0.05, 20 + BLINK_HOLD_MS - 1), true);  // dentro do hold
  assert.equal(gate.update(0.05, 20 + BLINK_HOLD_MS + 1), false); // hold expirou
});

test('a new blink during hold re-enters rejection', () => {
  const gate = createBlinkGateTracker();
  gate.update(0.9, 0);
  gate.update(0.1, 20);              // hold
  assert.equal(gate.update(0.8, 40), true);
  assert.equal(gate.update(0.4, 60), true); // e a histerese vale de novo
});

test('null score never starts a rejection (fail-open) but does not abort a blink', () => {
  const gate = createBlinkGateTracker();
  assert.equal(gate.update(null, 0), false);
  gate.update(0.9, 16);
  assert.equal(gate.update(null, 33), true); // null no meio da piscada → hold, não reabre
});

test('disabled tracker never rejects and reset clears state', () => {
  const off = createBlinkGateTracker({ enabled: false });
  assert.equal(off.update(0.99, 0), false);
  const gate = createBlinkGateTracker();
  gate.update(0.9, 0);
  gate.reset();
  assert.equal(gate.update(0.3, 16), false); // estado limpo: 0.3 não rejeita
});

test('purgeLeadingBlinkSamples drops only the trailing window in place', () => {
  const samples = [{ t: 0 }, { t: 400 }, { t: 950 }, { t: 990 }];
  purgeLeadingBlinkSamples(samples, 1000, BLINK_LEADING_PURGE_MS);
  assert.deepEqual(samples, [{ t: 0 }, { t: 400 }]); // 950/990 estão nos últimos 80ms
});
```

- [ ] **2. Rodar e ver falhar** — `node --import tsx --test src/services/blinkGate.test.ts` → `Cannot find module './blinkGate'`
- [ ] **3. Implementação mínima**

```ts
// src/services/blinkGate.ts
// Stateful blink gate: the binary threshold catches the middle of a blink but lets
// the partially-occluded edge frames through (score rising/falling through 0.3–0.5).
// At 60fps there are twice as many edge frames per second, so the edges dominate the
// artifact budget. Hysteresis catches the falling edge, the temporal hold covers the
// re-opening wobble, and the leading purge retroactively removes the rising edge from
// sample buffers. All windows are in ms, not frames — fps-independent by design.
import { BLINK_REJECT_THRESHOLD, BLINK_REJECT_GATE_ENABLED, isBlinking } from './faceTracking';

export const BLINK_EXIT_THRESHOLD = 0.25;
export const BLINK_HOLD_MS = 100;
export const BLINK_LEADING_PURGE_MS = 80;

export interface BlinkGateOptions {
  enterThreshold?: number;
  exitThreshold?: number;
  holdMs?: number;
  enabled?: boolean;
}

export interface BlinkGateTracker {
  /** true = descartar a amostra de gaze deste frame. */
  update(score: number | null, tMs: number): boolean;
  reset(): void;
}

export function createBlinkGateTracker(opts: BlinkGateOptions = {}): BlinkGateTracker {
  const enter = opts.enterThreshold ?? BLINK_REJECT_THRESHOLD;
  const exit = opts.exitThreshold ?? BLINK_EXIT_THRESHOLD;
  const holdMs = opts.holdMs ?? BLINK_HOLD_MS;
  const enabled = opts.enabled ?? BLINK_REJECT_GATE_ENABLED;

  let state: 'open' | 'closed' | 'hold' = 'open';
  let holdUntil = 0;

  return {
    update(score, tMs) {
      if (!enabled) return false;
      if (state === 'open') {
        if (isBlinking(score, enter)) { state = 'closed'; return true; }
        return false;
      }
      if (state === 'closed') {
        // Fail-open só para ENTRAR: um null no meio da piscada vira hold, não reabertura.
        if (score == null || score <= exit) { state = 'hold'; holdUntil = tMs + holdMs; }
        return true;
      }
      // state === 'hold'
      if (isBlinking(score, enter)) { state = 'closed'; return true; }
      if (tMs < holdUntil) return true;
      state = 'open';
      return false;
    },
    reset() { state = 'open'; holdUntil = 0; },
  };
}

// Remove in place as amostras dos últimos windowMs — a borda de subida da piscada que
// entrou no buffer antes do score cruzar o enter threshold. Buffers são ordenados por t.
export function purgeLeadingBlinkSamples(
  samples: Array<{ t: number }>,
  nowMs: number,
  windowMs: number = BLINK_LEADING_PURGE_MS,
): void {
  while (samples.length && nowMs - samples[samples.length - 1].t <= windowMs) samples.pop();
}
```

- [ ] **4. Rodar e ver passar** — `node --import tsx --test src/services/blinkGate.test.ts`
- [ ] **5. Rodar suite inteira** — `npm test` (os testes existentes de `faceTracking.test.ts` seguem passando: helpers intocados)
- [ ] **6. Commit** (se autorizado): `feat: blink gate tracker with hysteresis, temporal hold and leading purge`

---

### Task 3: Fiação — useCameraPipeline usa o tracker

**Arquivos:**
- Modificar: `src/hooks/useCameraPipeline.ts:9` (import), `:148` (uso), `:171-178` (reset)

- [ ] **1. Trocar import**: remover `shouldDropGazeForBlink` do import de `./faceTracking` (linha 9, manter `getBlinkScore`); adicionar `import { createBlinkGateTracker } from '@/services/blinkGate';` (conferir o alias usado pelos imports vizinhos do arquivo — se forem relativos, usar `../services/blinkGate`).
- [ ] **2. Criar o tracker como ref** junto aos outros refs do hook:

```ts
const blinkGateRef = useRef(createBlinkGateTracker());
```

- [ ] **3. Substituir na linha 148**:

```ts
const blinkScore = getBlinkScore();
const blinking = blinkGateRef.current.update(blinkScore, ts);
```

- [ ] **4. Resetar no início do loop** (dentro de `startFrameLoop`, junto de `frameTimesRef.current = []`):

```ts
blinkGateRef.current.reset();
```

- [ ] **5. Validar** — `npm test` + `npx tsc --noEmit` (se o projeto tiver script de typecheck, usar o script)
- [ ] **6. Commit** (se autorizado): `fix: camera pipeline blink gate with hysteresis and hold`

---

### Task 4: EyeTrackingTestScreen — EMA gateado, dt-compensado e purge do traçado

**Arquivos:**
- Modificar: `src/screens/EyeTrackingTestScreen.tsx` (~linhas 450-575)

- [ ] **1. Imports**: adicionar `import { emaAlpha, RAW_V_EMA_TAU_MS, DISTANCE_EMA_TAU_MS } from '@/services/emaTiming';` e `import { purgeLeadingBlinkSamples } from '@/services/blinkGate';` (mesmo estilo de alias dos imports vizinhos).
- [ ] **2. Refs novos** junto aos refs existentes do componente:

```ts
const lastFrameTsRef = useRef<number | null>(null);
const prevBlinkingRef = useRef(false);
```

- [ ] **3. dt no topo do `handleFrame`** (logo após `const { ts, video, pose, gaze, faceFound, eyesFound, blinking } = frame;`):

```ts
const dtMs = lastFrameTsRef.current != null ? ts - lastFrameTsRef.current : null;
lastFrameTsRef.current = ts;
// Borda de subida da piscada: as amostras dos últimos ~80ms já entraram no traçado
// com a íris parcialmente coberta (score ainda abaixo do enter threshold) — remover.
if (blinking && !prevBlinkingRef.current) {
  purgeLeadingBlinkSamples(visualSignalSamplesRef.current, ts);
}
prevBlinkingRef.current = blinking;
```

- [ ] **4. EMA de distância dt-compensado** (linha ~460, substituir `distanceRef.current = distanceRef.current * 0.85 + dEst * 0.15; // EMA smoothing`):

```ts
const distAlpha = dtMs == null ? 1 : emaAlpha(dtMs, DISTANCE_EMA_TAU_MS);
distanceRef.current = distanceRef.current + (dEst - distanceRef.current) * distAlpha;
```

- [ ] **5. EMA do trilho âmbar: gatear com `!blinking` + dt-compensar** (linhas ~556-562, substituir o bloco `if (gaze) { rawVEmaRef.current = ... }`):

```ts
// Slow EMA (~1.5s) of the raw vertical ratio: the amber dot's resting line.
// Blink frames must NOT enter the rail — the calibrated dot rides it (renderY),
// so an unguarded EMA turns every eyelid dip into vertical dot motion.
if (gaze && !blinking) {
  rawVEmaRef.current = rawVEmaRef.current == null || dtMs == null
    ? gaze.v
    : rawVEmaRef.current + (gaze.v - rawVEmaRef.current) * emaAlpha(dtMs, RAW_V_EMA_TAU_MS);
}
```

- [ ] **6. Validar** — `npm test`; `npm run build` (com `APP_BASE_PATH=/gaze` conforme o script de build do projeto); smokes: `node scripts/smoke-layout.mjs` e `node scripts/smoke-loading.mjs` (usar os invocadores que o BACKLOG registra como gate)
- [ ] **7. Commit** (se autorizado): `fix: blink-clean, fps-independent amber rail and distance smoothing`

---

### Task 5: ExerciseCanvas — tracker próprio no loop do exercício

**Arquivos:**
- Modificar: `src/components/ExerciseCanvas.tsx:5` (import), `~76` (criação), `~254` (uso)

- [ ] **1. Import**: remover `shouldDropGazeForBlink` do import de `faceTracking` (linha 5, manter `getBlinkScore`); adicionar import de `createBlinkGateTracker` de `blinkGate` (mesmo alias dos vizinhos).
- [ ] **2. Criar o tracker dentro do effect do exercício**, junto de `const stimulusTracker = createStimulusDistanceTracker(...)` (linha ~76):

```ts
const blinkGate = createBlinkGateTracker();
```

- [ ] **3. Substituir o uso** (linha ~254, dentro do loop que tem `exContext.timeMs` disponível):

```ts
const blinking = blinkGate.update(getBlinkScore(), exContext.timeMs);
```

- [ ] **4. Validar** — `npm test` + typecheck
- [ ] **5. Commit** (se autorizado): `fix: exercise capture blink gate with hysteresis and hold`

---

### Task 6: CalibrationOverlay — tracker próprio no loop de calibração

**Arquivos:**
- Modificar: `src/components/CalibrationOverlay.tsx:2` (import), effect do loop (~250), `:279` (uso)

- [ ] **1. Import**: remover `shouldDropGazeForBlink` (manter `getBlinkScore`); adicionar `createBlinkGateTracker`.
- [ ] **2. Criar o tracker no mesmo escopo onde `loop` é definido** (antes de `const loop = () => {`, dentro do effect):

```ts
const blinkGate = createBlinkGateTracker();
```

- [ ] **3. Substituir o uso** (linha ~279 — `now` de `performance.now()` já existe no escopo):

```ts
const blinking = blinkGate.update(getBlinkScore(), now);
```

O comentário existente sobre o timeout de ponto continua válido: baseline alto de piscada vira rejeição explícita do ponto, não modelo contaminado. A histerese/hold só muda **quais** frames são rejeitados, não a política de timeout.

- [ ] **4. Validar** — `npm test` + typecheck
- [ ] **5. Commit** (se autorizado): `fix: calibration blink gate with hysteresis and hold`

---

### Task 7: stimulusDistance — EMA dt-compensado opcional

**Arquivos:**
- Modificar: `src/services/stimulusDistance.ts:19-27` (options), `:29-47` (estado), `:69-72` (update)
- Modificar: `src/components/ExerciseCanvas.tsx:76` (call site passa `emaTauMs`)
- Teste: `src/services/stimulusDistance.test.ts` (adicionar caso)

- [ ] **1. Escrever teste que falha** (adicionar ao arquivo de teste existente, seguindo o estilo local):

```ts
test('emaTauMs makes the distance EMA frame-rate independent', () => {
  // Mesmo 1s de amostras a 55cm partindo de 40cm, em passos de 33.3ms vs 16.7ms.
  const mk = () => createStimulusDistanceTracker({
    profileDistanceCm: 40,
    emaTauMs: 200,
    freezeTimeoutMs: 10_000, // não congelar durante o teste
    convergenceWindowMs: 10_000,
  });
  const t30 = mk(); const t60 = mk();
  t30.update(40, 0); t60.update(40, 0);
  for (let i = 1; i <= 30; i++) t30.update(55, i * (100 / 3));
  for (let i = 1; i <= 60; i++) t60.update(55, i * (100 / 6));
  const d30 = t30.snapshot(); const d60 = t60.snapshot();
  // Ambos ainda 'stabilizing'; comparar o EMA via deviation não exposta — então
  // congelar agora e comparar o frozen: update com timeout vencido congela o EMA.
  const f30 = mk(); const f60 = mk();
  f30.update(40, 0); f60.update(40, 0);
  for (let i = 1; i <= 30; i++) f30.update(55, i * (100 / 3));
  for (let i = 1; i <= 60; i++) f60.update(55, i * (100 / 6));
  const s30 = f30.update(55, 20_000); // timeout 10s vencido → freeze do EMA
  const s60 = f60.update(55, 20_000);
  assert.ok(Math.abs(s30.frozenDistanceCm! - s60.frozenDistanceCm!) < 0.5);
  assert.equal(d30.phase, 'stabilizing');
  assert.equal(d60.phase, 'stabilizing');
});
```

*(Atenção do executor: o freeze em `t=20_000` também aplica um passo de EMA com dt=19s — alpha≈1 — antes de congelar, então ambos convergem a ~55; o assert compara os dois entre si, não contra um valor absoluto. Se o teste existente do arquivo importar helpers próprios, seguir o padrão local.)*

- [ ] **2. Rodar e ver falhar** — `node --import tsx --test src/services/stimulusDistance.test.ts` → falha porque `emaTauMs` não existe nas options (typecheck) / comportamento legado diverge
- [ ] **3. Implementação mínima** em `stimulusDistance.ts`:

Na interface (linha ~21):

```ts
export interface StimulusDistanceOptions {
  profileDistanceCm: number;
  /** Legado: alpha fixo por chamada (fps-dependente). Ignorado quando emaTauMs é dado. */
  emaAlpha?: number;
  /** Constante de tempo do EMA em ms — alpha = 1 − exp(−dt/τ), fps-independente. */
  emaTauMs?: number;
  convergenceWindowMs?: number;
  convergenceSpanPct?: number;
  freezeTimeoutMs?: number;
  driftEnterPct?: number;
  driftExitPct?: number;
}
```

No estado do tracker (junto de `let lastT`):

```ts
let lastUpdateT: number | null = null;
```

No `update` (substituir a linha do EMA, linha ~72):

```ts
const dt = lastUpdateT != null ? tMs - lastUpdateT : null;
lastUpdateT = tMs;
const alphaNow = opts.emaTauMs != null && dt != null && dt > 0
  ? 1 - Math.exp(-dt / opts.emaTauMs)
  : alpha;
if (validSample) ema = ema == null ? sampleCm! : ema + (sampleCm! - ema) * alphaNow;
```

No call site `ExerciseCanvas.tsx:76`:

```ts
const stimulusTracker = createStimulusDistanceTracker({ profileDistanceCm: viewingDistanceCm, emaTauMs: DISTANCE_EMA_TAU_MS });
```

(importar `DISTANCE_EMA_TAU_MS` de `emaTiming`.)

- [ ] **4. Rodar e ver passar** — `node --import tsx --test src/services/stimulusDistance.test.ts` e depois `npm test` (testes legados do tracker sem `emaTauMs` seguem no alpha fixo)
- [ ] **5. Commit** (se autorizado): `feat: fps-independent stimulus distance smoothing`

---

### Task 8: Fonte estável — congelar em vez de seguir a detecção

**Arquivos:**
- Modificar: `src/screens/EyeTrackingTestScreen.tsx` (~linhas 457-467)

Hoje o `fontPx` fora da captura segue `distanceRef` (EMA ao vivo) — o texto "respira" com a detecção. O player de exercícios já resolve isso com `createStimulusDistanceTracker` (mede, congela, só observa drift). Mesma política aqui: a fonte começa no tamanho do perfil, ajusta **uma vez** quando a medição estabiliza (~1-3s) e nunca mais se move; o EMA ao vivo continua existindo só para o check de tolerância de distância.

- [ ] **1. Import**: adicionar `createStimulusDistanceTracker` de `stimulusDistance` (mesmo alias dos vizinhos).
- [ ] **2. Ref do tracker** junto aos refs do componente:

```ts
const screenDistanceTrackerRef = useRef<ReturnType<typeof createStimulusDistanceTracker> | null>(null);
```

- [ ] **3. No `handleFrame`**, substituir o cálculo do `fontPx` (linhas ~464-467):

```ts
// Fonte estável: mede a distância, congela após convergir e não segue mais a
// detecção — texto que "respira" com o tracking é inutilizável para leitura.
// O EMA ao vivo (distanceRef) segue existindo só para o check de tolerância.
if (!screenDistanceTrackerRef.current) {
  screenDistanceTrackerRef.current = createStimulusDistanceTracker({
    profileDistanceCm: profileDistanceRef.current,
    emaTauMs: DISTANCE_EMA_TAU_MS,
  });
}
const stimulusSnap = screenDistanceTrackerRef.current.update(ipdPx != null && anchor ? dEst : null, ts);
const fontPx = capturingRef.current && frozenFontPxRef.current != null
  ? frozenFontPxRef.current
  : Math.round(readingFontCssPx(fontAngleRef.current, stimulusSnap.distanceCm));
```

*(`stimulusSnap.distanceCm` retorna a distância do perfil enquanto `stabilizing` — fonte estável desde o primeiro frame — e o valor congelado depois; `frozenFontPxRef` da captura continua como está, vira redundância inofensiva.)*

- [ ] **4. Validar** — `npm test` + typecheck + build. Sem teste unitário de screen (convenção do repo): o comportamento congelar-após-convergir é o do tracker já testado (Task 7); a fiação é validada no gate manual.
- [ ] **5. Commit** (se autorizado): `fix: freeze reading font size instead of tracking live detection`

---

### Task 9: Gate final do BUNDLE

- [ ] **1.** `npm test` — esperado: 395 existentes + novos, 0 fail
- [ ] **2.** `npm run lint`
- [ ] **3.** Build prefixado (`APP_BASE_PATH=/gaze`, conforme script do projeto) — build limpo
- [ ] **4.** Smokes do BACKLOG (layout, loading, notebook, phone-portrait) — mesmos invocadores registrados na última entrega
- [ ] **5.** Atualizar `BACKLOG.md` com a entrega + evidência real (números observados)
- [ ] **6.** **Pronto pra revisão** — gate manual de Anders: piscar no desktop 60fps e confirmar que o dot azul não mergulha e o traçado não mostra spike de borda; confirmar que o texto ajusta no máximo uma vez (ao estabilizar) e depois fica imóvel; reportar o valor de "Distância" do HUD (letras enormes: quanto é feature-distância vs. premissa 96dpi fica quantificável com esse número)

## Riscos (detectar cedo)

- **Hold 100ms agressivo demais** (piscadas em rajada → buracos longos no traçado): visível no gate manual; ajustar `BLINK_HOLD_MS` para 60-80ms é mudança de 1 constante testada.
- **Exit 0.25 abaixo do baseline do iPhone** (baseline ~0.3 deixaria o gate preso fechado): o timeout de ponto da calibração já transforma isso em rejeição explícita (comportamento documentado no BACKLOG de 21/07); se aparecer, subir `BLINK_EXIT_THRESHOLD` é 1 constante.
- **Teste da Task 7 sensível ao freeze**: se o assert do frozen flakear, comparar os EMAs congelando com dt pequeno (duas chamadas extras) em vez de dt=19s.
